<form method="post" action="{{ path('app_repair_record_delete', {'id': repair_record.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repair_record.id) }}">
    <button class="btn px-4 py-2 bg-red-600 hover:bg-red-500 text-white text-sm font-medium rounded-lg transition">Delete</button>
</form>
